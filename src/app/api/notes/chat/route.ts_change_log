Updated the chat backend to dynamically intercept markdown figure paths (e.g. `![alt](/figures/...)`) in user messages. The server now loads these figures from the local disk, converts them to base64, and injects them securely into the Ollama JSON payload. This allows for multimodal analysis without encountering frontend payload limits or requiring complicated UI state management for base64 arrays.
